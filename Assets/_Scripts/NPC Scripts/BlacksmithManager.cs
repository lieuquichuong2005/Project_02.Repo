using UnityEngine;

public class BlacksmithManager : MonoBehaviour
{/*
    public static BlacksmithManager instance;
    public DialogueManager dialogueManager;

    public Sprite npcAvatar;
    public string npcName = "Blacksmith";
    public string[] dialogues;

    private void Awake()
    {
        BlacksmithManager.instance = this;
    }
    public void Speak(int dialogueIndex)
    {
        if (dialogueIndex >= 0 && dialogueIndex < dialogues.Length)
        {
            dialogueManager.DisplayDialogue(npcName, dialogues[dialogueIndex]);
        }
    }
    */
}
